# Example App Template

Copy this directory to a new top-level app id directory, then replace every placeholder before publishing.

Top-level non-hidden directories are treated as app packages. Keep templates and helper files in hidden directories.

