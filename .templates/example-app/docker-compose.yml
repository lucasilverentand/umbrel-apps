version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: example-app_server_1
      APP_PORT: 8080

  server:
    image: nginx:1.27-alpine
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/usr/share/nginx/html

